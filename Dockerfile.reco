# Version simplifiée - Build complet en une seule commande
FROM maven:3.9-eclipse-temurin-17-alpine AS build

# Copier tout le projet
WORKDIR /workspace
COPY . .

# Build seulement reco-service
WORKDIR /workspace
RUN mvn clean install -pl reco-service -am -DskipTests

# Run stage
FROM eclipse-temurin:17-jre-alpine
WORKDIR /app
COPY --from=build /workspace/reco-service/target/*.jar app.jar
EXPOSE 8084
ENTRYPOINT ["java", "-jar", "app.jar"]
